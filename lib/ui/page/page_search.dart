import 'package:flutter/material.dart';

class PageSearchWidget extends StatelessWidget {
//  final itemData;
//
//  PageSearchWidget(this.itemData);

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      body: new Center(
        child: new Text("搜索"),
      ),
    );
  }
}
